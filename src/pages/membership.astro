---
import BaseLayout from '../layouts/BaseLayout.astro';
import Navigation from '../components/Navigation.astro';

const membershipPlans = [
  {
    id: 'individual',
    name: 'Individual Premiere',
    description: 'We drive you home in your own vehicle',
    price: '$89',
    priceSubtext: '/month - Often cheaper than taxi',
    features: [
      'We drive your own car home',
      'Unmarked vehicles (discrete service)',
      '3PM - 3AM availability', 
      'Professional, licensed drivers',
      'Greater Phoenix Area coverage',
      'No embarrassing pickup'
    ],
    popular: false,
    ctaText: 'Join Individual Plan',
    ctaUrl: '#' // To be replaced with AcceptBlue URL
  },
  {
    id: 'joint',
    name: 'Joint Premiere',
    description: 'Shared premium service for couples',
    price: '$149',
    priceSubtext: '/month - Best value for two people',
    features: [
      'Service for two individuals',
      'Both get home in own vehicles',
      'Shared membership benefits',
      'Priority scheduling',
      'Owner-operated service',
      'Phoenix local expertise'
    ],
    popular: true,
    ctaText: 'Join Joint Plan',
    ctaUrl: '#' // To be replaced with AcceptBlue URL
  },
  {
    id: 'family',
    name: 'Friends & Family Premiere',
    description: 'Premium service for your inner circle',
    price: '$249',
    priceSubtext: '/month - Up to 6 members included',
    features: [
      'Service for up to 6 members',
      'Everyone gets home in own cars',
      'Group event coordination',
      'Shared membership value',
      'Professional discrete service',
      'Community safety focus'
    ],
    popular: false,
    ctaText: 'Join Family Plan',
    ctaUrl: '#' // To be replaced with AcceptBlue URL
  },
  {
    id: 'corporate',
    name: 'Corporate Premiere',
    description: 'Professional business transportation',
    price: '$399',
    priceSubtext: '/month - Multi-employee coverage',
    features: [
      'Multi-employee coverage',
      'Executive-level discrete service',
      'Business event coordination',
      'Reduces company liability',
      'Professional representation',
      'Arizona local expertise'
    ],
    popular: false,
    ctaText: 'Join Corporate Plan',
    ctaUrl: '#' // To be replaced with AcceptBlue URL
  },
  {
    id: 'business',
    name: 'Bar/Restaurant Partnership',
    description: 'Partner with us to offer your customers safe rides',
    price: 'Custom',
    priceSubtext: 'Revenue share model available',
    features: [
      'Customer service integration',
      'Branded partnership opportunity',
      'Liability reduction for business',
      'Customer retention benefits',
      'Professional service standards',
      'Local Phoenix market focus'
    ],
    popular: false,
    ctaText: 'Partner With Us',
    ctaUrl: '#' // To be replaced with AcceptBlue URL
  }
];

const serviceAddOns = [
  {
    name: 'Airport Transportation',
    description: 'Get to/from Sky Harbor in your own vehicle',
    icon: '✈️'
  },
  {
    name: 'Car Retrieval Service',
    description: 'We\'ll get your car from where you left it',
    icon: '🚗'
  },
  {
    name: 'Chauffeur Services',
    description: 'Professional driver for special occasions',
    icon: '🤵'
  },
  {
    name: 'Professional Coverage',
    description: 'Phoenix, Scottsdale, Paradise Valley area',
    icon: '📍'
  }
];
---

<BaseLayout 
  title="Membership Plans - Chasers DD Luxury Service"
  description="Choose from our premium membership tiers: Individual, Joint, Friends & Family, or Corporate. Professional designated driver service with luxury vehicles and 24/7 availability."
  keywords="membership plans, luxury transportation, designated driver membership, premium service tiers, corporate transportation"
>
  <Navigation />
  
  <main class="min-h-screen bg-gradient-to-b from-black via-gray-900 to-black">
    <!-- Hero Section -->
    <section class="pt-32 pb-16 px-4 sm:px-6 lg:px-8">
      <div class="max-w-7xl mx-auto text-center">
        <h1 class="text-4xl md:text-6xl font-bold text-white mb-6">
          <span class="font-serif">Premiere</span>
          <span class="text-[#c69214] ml-3">Membership</span>
        </h1>
        <p class="text-xl md:text-2xl text-gray-300 max-w-3xl mx-auto leading-relaxed mb-6">
          We drive you home in your own vehicle. Often cheaper than taxi service 
          with the convenience and affordability that makes it almost a necessity.
        </p>
        <div class="bg-gradient-to-r from-[#c69214]/20 to-[#b8841c]/20 border border-[#c69214]/30 rounded-xl p-6 max-w-2xl mx-auto">
          <p class="text-lg text-[#c69214] font-semibold mb-2">Service Hours: 3PM - 3AM Daily</p>
          <p class="text-gray-300">Greater Phoenix Area | Call (480) 695-3659</p>
        </div>
      </div>
    </section>

    <!-- Membership Plans Grid -->
    <section class="py-16 px-4 sm:px-6 lg:px-8">
      <div class="max-w-7xl mx-auto">
        <div class="flex flex-wrap justify-center gap-6 lg:gap-8">
          {membershipPlans.map((plan) => (
            <div class={`relative bg-gradient-to-b from-gray-800 to-gray-900 rounded-2xl p-6 lg:p-8 border transition-all duration-300 hover:scale-105 hover:shadow-2xl flex flex-col w-full sm:w-80 md:w-72 lg:w-80 xl:w-72 ${
              plan.popular 
                ? 'border-[#c69214] shadow-lg shadow-[#c69214]/20' 
                : 'border-gray-700 hover:border-[#c69214]/50'
            }`}>
              {plan.popular && (
                <div class="absolute -top-4 left-1/2 transform -translate-x-1/2">
                  <span class="bg-gradient-to-r from-[#c69214] to-[#b8841c] text-black px-3 py-1.5 rounded-full text-xs font-bold whitespace-nowrap">
                    MOST POPULAR
                  </span>
                </div>
              )}
              
              <div class="text-center mb-6 lg:mb-8">
                <h3 class="text-xl lg:text-2xl font-bold text-white mb-2 font-serif">{plan.name}</h3>
                <p class="text-gray-400 text-sm mb-4">{plan.description}</p>
                <div class="text-2xl font-bold text-[#c69214] mb-2">{plan.price}</div>
                {plan.priceSubtext && (
                  <p class="text-sm text-gray-500 mb-4">{plan.priceSubtext}</p>
                )}
              </div>

              <ul class="space-y-3 lg:space-y-4 mb-6 lg:mb-8 flex-grow">
                {plan.features.map((feature) => (
                  <li class="flex items-start text-gray-300">
                    <svg class="w-5 h-5 text-[#c69214] mr-3 mt-0.5 flex-shrink-0" fill="currentColor" viewBox="0 0 20 20">
                      <path fill-rule="evenodd" d="M16.707 5.293a1 1 0 010 1.414l-8 8a1 1 0 01-1.414 0l-4-4a1 1 0 011.414-1.414L8 12.586l7.293-7.293a1 1 0 011.414 0z" clip-rule="evenodd" />
                    </svg>
                    <span class="text-sm leading-relaxed">{feature}</span>
                  </li>
                ))}
              </ul>

              <a 
                href={plan.ctaUrl}
                target="_blank"
                rel="noopener noreferrer" 
                class={`block w-full text-center py-4 px-6 rounded-xl font-bold transition-all duration-300 ${
                  plan.popular
                    ? 'bg-gradient-to-r from-[#c69214] to-[#b8841c] text-black hover:from-[#b8841c] hover:to-[#a67619] transform hover:scale-105'
                    : 'bg-gradient-to-r from-gray-700 to-gray-800 text-white hover:from-[#c69214] hover:to-[#b8841c] hover:text-black'
                }`}
              >
                {plan.ctaText.includes('Plan') ? (
                  <>
                    <span class="block">{plan.ctaText.replace(' Plan', '')}</span>
                    <span class="block">Plan</span>
                  </>
                ) : (
                  <span class="block">{plan.ctaText}</span>
                )}
              </a>
            </div>
          ))}
        </div>
      </div>
    </section>

    <!-- Service Add-ons Section -->
    <section class="py-16 px-4 sm:px-6 lg:px-8 bg-gray-900/50">
      <div class="max-w-7xl mx-auto">
        <div class="text-center mb-12">
          <h2 class="text-3xl md:text-4xl font-bold text-white mb-4 font-serif">
            Premium <span class="text-[#c69214]">Add-ons</span>
          </h2>
          <p class="text-xl text-gray-300 max-w-2xl mx-auto">
            Enhance your membership with our exclusive service upgrades
          </p>
        </div>

        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6">
          {serviceAddOns.map((addon) => (
            <div class="bg-gradient-to-b from-gray-800 to-gray-900 rounded-xl p-6 border border-gray-700 hover:border-[#c69214]/50 transition-all duration-300 hover:scale-105 text-center">
              <div class="text-4xl mb-4">{addon.icon}</div>
              <h3 class="text-lg font-bold text-white mb-2">{addon.name}</h3>
              <p class="text-gray-400 text-sm">{addon.description}</p>
            </div>
          ))}
        </div>
      </div>
    </section>

    <!-- CTA Section -->
    <section class="py-16 px-4 sm:px-6 lg:px-8">
      <div class="max-w-4xl mx-auto text-center">
        <h2 class="text-3xl md:text-4xl font-bold text-white mb-6 font-serif">
          Ready to Experience <span class="text-[#c69214]">Luxury?</span>
        </h2>
        <p class="text-xl text-gray-300 mb-8 max-w-2xl mx-auto">
          Join the owner-operated service that really cares. Professional, discrete, and often cheaper than traditional alternatives.
        </p>
        <div class="flex flex-col sm:flex-row gap-4 justify-center">
          <a 
            href="/contact" 
            class="bg-gradient-to-r from-[#c69214] to-[#b8841c] text-black px-8 py-4 rounded-xl font-bold hover:from-[#b8841c] hover:to-[#a67619] transition-all duration-300 transform hover:scale-105"
          >
            Contact Us Today
          </a>
          <a 
            href="/how-it-works" 
            class="bg-gradient-to-r from-gray-700 to-gray-800 text-white px-8 py-4 rounded-xl font-bold hover:from-[#c69214] hover:to-[#b8841c] hover:text-black transition-all duration-300"
          >
            Learn How It Works
          </a>
        </div>
      </div>
    </section>
  </main>
</BaseLayout>